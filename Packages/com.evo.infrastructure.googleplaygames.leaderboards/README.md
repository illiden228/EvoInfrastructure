# Google Play Games Leaderboards

```csharp
features.UseLeaderboards();
features.UseGooglePlayGames();
features.UseGooglePlayGamesLeaderboards(options);
```

Configure logical gameplay keys to Google leaderboard resource IDs. Install the official SDK separately and enable `EVO_GOOGLE_PLAY_GAMES_SDK`.
