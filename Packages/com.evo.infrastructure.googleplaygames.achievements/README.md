# Google Play Games Achievements

```csharp
features.UseAchievements();
features.UseGooglePlayGames();
features.UseGooglePlayGamesAchievements(options);
```

The options map logical achievement keys to Google resource IDs. The official SDK is installed separately.
