# CrazyGames Identity
Register with `features.UseIdentity(); features.UseCrazyGamesIdentity();`. The CrazyGames SDK is supplied separately by `com.evo.infrastructure.crazygames`.

The current CrazyGames `PortalUser` API does not expose a stable player ID. Username is used as a provider-local fallback and must not be used for account linking.
