# Odin Package Source

Place an Odin Inspector `.unitypackage` in this folder to let `EvoTools/Setup` import Odin automatically.

Expected pattern:

```text
Odin*.unitypackage
```

Do not commit or distribute Odin here unless your team/license allows distributing that package through this repository.
