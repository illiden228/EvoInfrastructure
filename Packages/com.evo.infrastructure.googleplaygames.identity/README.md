# Google Play Games Identity

```csharp
features.UseIdentity();
features.UseGooglePlayGames();
features.UseGooglePlayGamesIdentity();
```

Automatic authentication is suitable for startup. Interactive authentication should only be requested from an explicit user action.
